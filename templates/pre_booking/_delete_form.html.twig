<form method="post" action="{{ path('app_pre_booking_delete', {'id': pre_booking.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pre_booking.id) }}">
    <button class="btn">Delete</button>
</form>
