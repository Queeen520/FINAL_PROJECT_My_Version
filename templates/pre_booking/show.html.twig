{% extends 'base.html.twig' %} {% block title %}PreBooking{% endblock %} {%
block body %}
<div class="container">
  <h1>PreBooking</h1>
  <div
    class="container mb-4 mt-4 p-4 border border-secondary rounded border-opacity-25">
    <table class="table">
      <tbody>
        <tr>
          <th>Id</th>
          <td>{{ pre_booking.id }}</td>
        </tr>
        <tr>
          <th>NumberParticipants</th>
          <td>{{ pre_booking.numberParticipants }}</td>
        </tr>
      </tbody>
    </table>

    <a href="{{ path('app_pre_booking_edit', { id: pre_booking.id }) }}"
      >Edit</a
    >

    {{ include("pre_booking/_delete_form.html.twig") }}
  </div>
  <a href="{{ path('app_pre_booking_index') }}">back to list</a>
</div>

{% endblock %}
