{% extends 'base.html.twig' %} {% block title %}Edit Price{% endblock %} {%
block body %}
<div class="container">
  <h1>Edit Price</h1>
  <div
    class="container mb-4 mt-4 p-4 border border-secondary rounded border-opacity-25">
    {{ include("price/_form.html.twig", { button_label: "Update" }) }}

    {{ include("price/_delete_form.html.twig", { button_label: "Delete" }) }}
  </div>
  <a href="{{ path('app_price_index') }}">Back</a>
</div>
{% endblock %}
