{% extends 'base.html.twig' %} {% block title %}write review{% endblock %} {% block body %} {% include 'user_navbar.html.twig' %}
<div class="container">
    <h1 class="d-flex justify-content-center">write review</h1>

    <div class="reg-form ">
        {{ form_start(reviewForm) }} 
       
        {{ form_row(reviewForm.email, { label: "email" }) }}
        {{ form_row(reviewForm.name, { label: "name" }) }}
        {{ form_row(reviewForm.message, { label: "message" }) }}
    </div>
    <div class="reg-form">
       
       {#  <button type="submit" class="btn btn-danger">send</button>  #}
        {{ form_end(reviewForm) }}
    </div>

    {# <a href="{{ path('app_login') }}">login</a>
    <a href="{{ path('app_register') }}">register</a>
    <a href="{{ path('app_logout') }}">logout</a> #}
</div>
{% endblock %}