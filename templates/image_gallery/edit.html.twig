{% extends 'base.html.twig' %} {% block title %}Edit Image Gallery{% endblock %}
{% block body %}
<div class="container">
  <h1>Edit Image Gallery</h1>
  <div
    class="container mb-4 mt-4 p-4 border border-secondary rounded border-opacity-25">
    {{ include("image_gallery/_form.html.twig", { button_label: "Update" }) }}

    {{ include("image_gallery/_delete_form.html.twig") }}
  </div>
  <a href="{{ path('app_image_gallery_index') }}">Back</a>
</div>
{% endblock %}
