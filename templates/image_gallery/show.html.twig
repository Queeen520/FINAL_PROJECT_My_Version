{% extends 'base.html.twig' %}

{% block title %}ImageGallery{% endblock %}

{% block body %}
    <h1>ImageGallery</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ image_gallery.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ image_gallery.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ image_gallery.image }}</td>
               <td> <img
        src="{{asset('pictures/') ~ image_gallery.image}}"
        alt="image"
        width="300" /></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_image_gallery_index') }}">back to list</a>

    <a href="{{ path('app_image_gallery_edit', {'id': image_gallery.id}) }}">edit</a>

    {{ include('image_gallery/_delete_form.html.twig') }}
{% endblock %}
